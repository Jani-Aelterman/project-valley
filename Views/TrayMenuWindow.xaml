<winuiex:WindowEx
    x:Class="NextValleyDock.Views.TrayMenuWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    xmlns:helpers="using:NextValleyDock.Helpers"
    mc:Ignorable="d"
    IsTitleBarVisible="False"
    IsMinimizable="False"
    IsMaximizable="False"
    IsResizable="False">

    <Grid x:Name="ThemeRoot" Background="Transparent" Padding="0">
        <Border CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="8">
            <StackPanel>
                <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{x:Bind TrayIcons}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VariableSizedWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="4" ItemHeight="40" ItemWidth="40"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="helpers:TrayIconData">
                                <Button Width="36" Height="36" Margin="2" Background="Transparent" BorderThickness="0" Padding="4" ToolTipService.ToolTip="{x:Bind ToolTip}">
                                    <Image Source="{x:Bind Icon, Mode=OneWay}" Stretch="Uniform" Width="20" Height="20" />
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
            </StackPanel>
        </Border>
    </Grid>
</winuiex:WindowEx>
